<Window x:Class="Planora.UI.View.ScheduleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:Planora.UI.Converters"
        mc:Ignorable="d"
        Title="Schedule"
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        Foreground="White"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <conv:BoolToViewModeConverter x:Key="BoolToViewModeConverter"/>
    </Window.Resources>

    <Border CornerRadius="20"
            BorderThickness="2"
            Padding="5">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#462AD8" Offset="0"/>
                <GradientStop Color="#DA34AE" Offset="0.75"/>
                <GradientStop Color="#8A16C1" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>

        <Border.Background>
            <LinearGradientBrush StartPoint="0,1" EndPoint="1,0">
                <GradientStop Color="#060531" Offset="0"/>
                <GradientStop Color="#490c82" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!-- Верхня панель з кнопками -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="25"/>
                    <ColumnDefinition Width="25"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Schedule"
                           Foreground="DarkGray"
                           FontSize="10"
                           FontFamily="Montserrat"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"/>

                <Button x:Name="btnMinimize"
                        BorderThickness="0"
                        Content="-"
                        Foreground="White"
                        FontSize="16"
                        FontFamily="Montserrat"
                        Cursor="Hand"
                        Grid.Column="1"
                        Click="btnMinimize_Click">
                    <Button.Background>
                        <SolidColorBrush Color="Transparent"/>
                    </Button.Background>
                </Button>

                <Button x:Name="btnClose"
                        BorderThickness="0"
                        Content="X"
                        Foreground="White"
                        FontSize="12"
                        FontFamily="Montserrat"
                        Cursor="Hand"
                        Grid.Column="2"
                        Click="btnClose_Click">
                    <Button.Background>
                        <SolidColorBrush Color="Transparent"/>
                    </Button.Background>
                </Button>
            </Grid>

            <!-- Основний контент -->
            <Grid Grid.Row="1" Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="83*"/>
                    <ColumnDefinition Width="199*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Панель керування -->
                <StackPanel Orientation="Horizontal" Margin="0,10,0,10" Grid.ColumnSpan="2">
                    <Button Content="◄ Попередній"
                            Command="{Binding PreviousPeriodCommand}"
                            Margin="0,0,10,0"
                            Click="btnPrevious_Click"
                            FontFamily="Montserrat" Width="101"/>
                    <TextBlock Text="{Binding PeriodDisplay}"
                               VerticalAlignment="Center"
                               FontSize="16"
                               FontFamily="Montserrat"
                               FontWeight="Medium"/>
                    <Button Content="Наступний ►"
                            Command="{Binding NextPeriodCommand}"
                            Margin="10,0,0,0"
                            Click="btnNext_Click"
                            FontFamily="Montserrat" Width="89"/>
                    <Button Content="Сьогодні"
                            Command="{Binding TodayCommand}"
                            Margin="20,0,0,0"
                            FontFamily="Montserrat" Width="67"/>
                    <Button Content="{Binding IsWeekView, Converter={StaticResource BoolToViewModeConverter}}"
                            Command="{Binding ToggleViewCommand}"
                            Margin="20,0,0,0"
                            FontFamily="Montserrat" Width="38"/>

                    <!-- Пошук -->
                    <Grid Width="200" Margin="30,0,0,0">
                        <TextBox x:Name="SearchBox"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Background="Transparent"
                                 Foreground="White"
                                 BorderBrush="Gray"
                                 BorderThickness="1"
                                 Padding="5"
                                 FontFamily="Montserrat"/>
                        <TextBlock Text="Пошук..."
                                   Foreground="Gray"
                                   Margin="8,2,0,0"
                                   IsHitTestVisible="False"
                                   VerticalAlignment="Center"
                                   FontFamily="Montserrat">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text.Length, ElementName=SearchBox}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Text.Length, ElementName=SearchBox}" Value="1">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </StackPanel>

                <!-- Таблиця -->
                <DataGrid
    ItemsSource="{Binding FilteredScheduleItems}"
    SelectedItem="{Binding SelectedScheduleItem}"
    AutoGenerateColumns="False"
    CanUserAddRows="False"
    HeadersVisibility="Column"
    GridLinesVisibility="None"
    BorderThickness="0"
    Background="#1E1E2F"
    RowBackground="#232336"
    AlternatingRowBackground="#2C2C3E"
    FontFamily="Montserrat"
    FontSize="14"
    Foreground="#E4E4E7"
    RowHeight="36"
    ColumnHeaderHeight="42"
    Margin="0,46,0,0"
    Grid.ColumnSpan="2"
    Grid.RowSpan="2"
    SnapsToDevicePixels="True"
    EnableRowVirtualization="True"
    SelectionMode="Single"
    SelectionUnit="FullRow">

                    <DataGrid.Resources>
                        <!-- 🔹 Заголовки колонок -->
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#2D2D44"/>
                            <Setter Property="Foreground" Value="#A5B4FC"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="BorderBrush" Value="#383850"/>
                            <Setter Property="BorderThickness" Value="0,0,0,2"/>
                            <Setter Property="Height" Value="42"/>
                            <Setter Property="FontSize" Value="13"/>
                        </Style>

                        <!-- 🔹 Рядки таблиці -->
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="#1E1E2F"/>
                            <Setter Property="BorderBrush" Value="#2F2F40"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <!-- Hover -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#3B3B5A"/>
                                </Trigger>
                                <!-- Вибраний рядок -->
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#6366F1"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>

                        <!-- 🔹 Комірки таблиці -->
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,0,10,0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.Resources>

                    <!-- 🔹 Колонки -->
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="День" Binding="{Binding Day}" Width="*" />
                        <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=dd.MM.yyyy}" Width="*" />
                        <DataGridTextColumn Header="Час" Binding="{Binding Time}" Width="*" />
                        <DataGridTextColumn Header="Предмет" Binding="{Binding Subject}" Width="2*" />
                        <DataGridTextColumn Header="Викладач" Binding="{Binding Teacher}" Width="2*" />
                        <DataGridTextColumn Header="Аудиторія" Binding="{Binding Classroom}" Width="*" />
                        <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="*" />
                        <DataGridTextColumn Header="Група" Binding="{Binding Group}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>

            </Grid>
        </Grid>
    </Border>
</Window>

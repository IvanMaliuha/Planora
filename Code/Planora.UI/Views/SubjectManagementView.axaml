<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Planora.ViewModels.ViewModels"
             x:Class="Planora.Views.SubjectManagementView"
             x:DataType="vm:SubjectManagementViewModel">

    <Grid RowDefinitions="Auto, *">
        <!-- ТУЛБАР -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Margin="0,0,0,20">
            <TextBlock Text="Предмети" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
            
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     Watermark="Пошук..." Width="200"/>
            
            <Button Command="{Binding AddSubjectCommand}" Content="+ Додати" Background="#2563EB" Foreground="White"/>
            
            <StackPanel Orientation="Horizontal" Spacing="10" IsVisible="{Binding SelectedSubject, Converter={x:Static ObjectConverters.IsNotNull}}">
                <Button Command="{Binding EditSubjectCommand}" Content="✏️ Редагувати"/>
                <Button Command="{Binding DeleteSubjectCommand}" Content="🗑️ Видалити" Background="#FEE2E2" Foreground="#DC2626"/>
            </StackPanel>
        </StackPanel>

        <!-- ТАБЛИЦЯ -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Subjects}" 
                  SelectedItem="{Binding SelectedSubject}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1" BorderBrush="#E5E7EB" CornerRadius="4">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Назва предмету" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Тип заняття" Binding="{Binding Type}" Width="*"/>
                <DataGridTextColumn Header="Кредити ECTS" Binding="{Binding Credits}" Width="0.8*"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- ФОРМА РЕДАГУВАННЯ -->
        <Grid Grid.RowSpan="2" Background="#99000000" IsVisible="{Binding IsEditMode}">
            <Border Background="White" Width="400" VerticalAlignment="Center" CornerRadius="10" Padding="20" BoxShadow="0 4 20 0 #30000000">
                <StackPanel Spacing="15">
                    <TextBlock Text="Редагування предмету" FontSize="20" FontWeight="Bold"/>
                    
                    <StackPanel>
                        <TextBlock Text="Назва предмету"/>
                        <TextBox Text="{Binding NewSubject.Name, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Тип заняття (Лекція/Практика)"/>
                        <ComboBox SelectedItem="{Binding NewSubject.Type, UpdateSourceTrigger=PropertyChanged}" 
                                  HorizontalAlignment="Stretch">
                            <ComboBoxItem>Лекція</ComboBoxItem>
                            <ComboBoxItem>Практика</ComboBoxItem>
                            <ComboBoxItem>Лабораторна</ComboBoxItem>
                            <ComboBoxItem>Семінар</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Кредити ECTS"/>
                        <NumericUpDown Value="{Binding NewSubject.Credits}" Minimum="0" Maximum="30"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="10" Margin="0,10,0,0">
                        <Button Command="{Binding CancelCommand}" Content="Скасувати"/>
                        <Button Command="{Binding SaveSubjectCommand}" Content="Зберегти" Background="#2563EB" Foreground="White"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
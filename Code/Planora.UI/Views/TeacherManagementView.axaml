<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Planora.ViewModels.ViewModels"
             x:Class="Planora.Views.TeacherManagementView"
             x:DataType="vm:TeacherManagementViewModel">

    <Grid RowDefinitions="Auto, *">
        <!-- ТУЛБАР -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Margin="0,0,0,20">
            <TextBlock Text="Викладачі" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
            
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     Watermark="Пошук..." Width="200"/>
            
            <Button Command="{Binding AddTeacherCommand}" Content="+ Додати" Background="#2563EB" Foreground="White"/>
            
            <StackPanel Orientation="Horizontal" Spacing="10" IsVisible="{Binding SelectedTeacher, Converter={x:Static ObjectConverters.IsNotNull}}">
                <Button Command="{Binding EditTeacherCommand}" Content="✏️ Редагувати"/>
                <Button Command="{Binding DeleteTeacherCommand}" Content="🗑️ Видалити" Background="#FEE2E2" Foreground="#DC2626"/>
            </StackPanel>
        </StackPanel>

        <!-- ТАБЛИЦЯ -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Teachers}" 
                  SelectedItem="{Binding SelectedTeacher}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1" BorderBrush="#E5E7EB" CornerRadius="4">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ПІБ" Binding="{Binding FullName}" Width="2*"/>
                <DataGridTextColumn Header="Кафедра" Binding="{Binding Department}" Width="*"/>
                <DataGridTextColumn Header="Посада" Binding="{Binding Position}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1.5*"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- ФОРМА РЕДАГУВАННЯ -->
        <Grid Grid.RowSpan="2" Background="#99000000" IsVisible="{Binding IsEditMode}">
            <Border Background="White" Width="400" VerticalAlignment="Center" CornerRadius="10" Padding="20" BoxShadow="0 4 20 0 #30000000">
                <StackPanel Spacing="15">
                    <TextBlock Text="Викладач" FontSize="20" FontWeight="Bold"/>
                    
                    <StackPanel>
                        <TextBlock Text="ПІБ"/>
                        <TextBox Text="{Binding NewTeacher.FullName, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Кафедра"/>
                        <TextBox Text="{Binding NewTeacher.Department, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Посада"/>
                        <ComboBox ItemsSource="{Binding AvailablePositions}"
                                SelectedItem="{Binding NewTeacher.Position}" 
                                HorizontalAlignment="Stretch"
                                PlaceholderText="Оберіть посаду..."/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Email"/>
                        <TextBox Text="{Binding NewTeacher.Email, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="10" Margin="0,10,0,0">
                        <Button Command="{Binding CancelCommand}" Content="Скасувати"/>
                        <Button Command="{Binding SaveTeacherCommand}" Content="Зберегти" Background="#2563EB" Foreground="White"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
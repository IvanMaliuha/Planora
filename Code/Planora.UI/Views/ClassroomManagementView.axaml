<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Planora.ViewModels.ViewModels"
             x:Class="Planora.Views.ClassroomManagementView"
             x:DataType="vm:ClassroomManagementViewModel">

    <Grid RowDefinitions="Auto, *">
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Margin="0,0,0,20">
            <TextBlock Text="Аудиторії" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
            
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     Watermark="Пошук..." Width="200"/>
            
            <ComboBox ItemsSource="{Binding FilterOptions}" SelectedItem="{Binding SelectedFilter}" Width="150"/>
            
            <Button Command="{Binding AddClassroomCommand}" Content="+ Додати" Background="#2563EB" Foreground="White"/>
            
            <StackPanel Orientation="Horizontal" Spacing="10" IsVisible="{Binding SelectedClassroom, Converter={x:Static ObjectConverters.IsNotNull}}">
                <Button Command="{Binding EditClassroomCommand}" Content="✏️ Редагувати"/>
                <Button Command="{Binding DeleteClassroomCommand}" Content="🗑️ Видалити" Background="#FEE2E2" Foreground="#DC2626"/>
            </StackPanel>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Classrooms}" 
                  SelectedItem="{Binding SelectedClassroom}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1" BorderBrush="#E5E7EB" CornerRadius="4">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер" Binding="{Binding Number}" Width="*"/>
                <DataGridTextColumn Header="Корпус" Binding="{Binding Building}" Width="*"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="1.5*"/>
                <DataGridTextColumn Header="Місць" Binding="{Binding Capacity}" Width="0.8*"/>
                <DataGridCheckBoxColumn Header="ПК" Binding="{Binding HasComputers}" Width="0.8*"/>
                <DataGridCheckBoxColumn Header="Проектор" Binding="{Binding HasProjector}" Width="0.8*"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <Grid Grid.RowSpan="2" Background="#99000000" IsVisible="{Binding IsEditMode}">
            <Border Background="White" Width="400" VerticalAlignment="Center" CornerRadius="10" Padding="20">
                <StackPanel Spacing="15">
                    <TextBlock Text="Редагування аудиторії" FontSize="20" FontWeight="Bold"/>
                    
                    <Grid ColumnDefinitions="*, 10, *">
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Номер"/>
                            <TextBox Text="{Binding NewClassroom.Number, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Корпус"/>
                            <TextBox Text="{Binding NewClassroom.Building, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel>
                        <TextBlock Text="Тип аудиторії"/>
                        <TextBox Text="{Binding NewClassroom.Type, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Місткість (осіб)"/>
                        <NumericUpDown Value="{Binding NewClassroom.Capacity, Mode=TwoWay}" Minimum="1"/>
                    </StackPanel>

                    <CheckBox IsChecked="{Binding NewClassroom.HasComputers}" Content="Наявність комп'ютерів"/>
                    <CheckBox IsChecked="{Binding NewClassroom.HasProjector}" Content="Наявність проектора"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="10" Margin="0,10,0,0">
                        <Button Command="{Binding CancelCommand}" Content="Скасувати"/>
                        <Button Command="{Binding SaveClassroomCommand}" Content="Зберегти" Background="#2563EB" Foreground="White"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>